# Intrusion-Detection-System-in-Python
A Python-based Intrusion Detection System (IDS) that analyzes network traffic from .pcap files and matches it against custom detection rules. Features include packet inspection for protocols (TCP, UDP, ICMP, IP), flag detection (SYN, ACK, etc.), malicious content detection, and logging of alerts with timestamps.
